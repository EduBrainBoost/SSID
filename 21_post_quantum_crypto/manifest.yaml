apiVersion: ssid/implementation/v1
kind: Manifest
metadata:
  name: 21-post-quantum-crypto
  version: "0.1.0"
  description: "Implementation manifest for 21 Post Quantum Crypto"
  created: "2025-10-13"
  status: bootstrap

spec:
  maintainer: "Cryptography Team"

  entry_points:
    primary: "pqc_keygen.py"
    secondary: \n      - signing_service.py

  dependencies:
    runtime:
      python: ">=3.11"

    build:
      - setuptools
      - wheel

    test:
      - pytest
      - pytest-cov

  build:
    command: "python -m pip install -e ."
    artifacts:
      - dist/
      - build/

  test:
    command: "pytest tests/ --cov=crypto"
    coverage_threshold: 80

  deployment:
    environments:
      - dev
      - stage
      - prod

    health_check:
      enabled: true
      endpoint: "/health"
      interval_seconds: 30

  observability:
    metrics:
      enabled: true
      port: 9090

    logging:
      level: INFO
      format: json

    tracing:
      enabled: true
      sample_rate: 0.1

  compliance:
    safe_fix: true
    root_24_lock: enforced
    pii_policy: hash_only

# SSID OPA Policy - 04_deployment (FUNCTIONAL v6.3)
# Auto-generated from 04_deployment/chart.yaml
# Mode: HONEST COMPLIANCE + ROOT-24-LOCK STRICT + SAFE-FIX
# Status: FUNCTIONAL

package ssid.04deployment.v6_0

# FUNCTIONAL POLICY - ready = true
default ready := true

# Metadata
metadata := {
  "root": "04_deployment",
  "version": "v6.0",
  "status": "functional",
  "capabilities_count": 6,
  "policies_count": 3,
  "interfaces_count": 3
}

# ============================================================
# FUNCTIONAL ENFORCEMENT RULES
# ============================================================

default allow := false

# ALLOW RULE 1: Valid authenticated operations
allow {
  input.request.type == "04_deployment_operation"
  input.request.valid == true
  input.auth.authenticated == true
}

# ALLOW RULE 2: Read operations (less restrictive)
allow {
  input.request.type == "read"
  input.resource.root == "04_deployment"
  input.auth.authenticated == true
}

# ALLOW RULE 3: Capability-based operations
allow {
  input.request.type == "capability_check"
  input.capability in ["infrastructure_as_code", "multi_environment_deployment", "blue_green_deployments", "canary_releases", "rollback_automation", "configuration_management"]
  input.auth.authorized == true
}

# DENY RULE 1: Missing authentication
deny[msg] {
  input.request.type == "04_deployment_operation"
  not input.auth.authenticated
  msg := "Authentication required for 04_deployment operations"
}

# DENY RULE 2: Invalid request
deny[msg] {
  input.request.type == "04_deployment_operation"
  input.request.valid == false
  msg := "Invalid request for 04_deployment - validation failed"
}

# DENY RULE 3: Missing required fields
deny[msg] {
  input.request.type == "04_deployment_operation"
  not input.request.action
  msg := "Missing required field: action"
}

# Policy-specific enforcement rules

# Policy: environment_isolation (enforcement: automated, scope: all_environments)
deny_environmentisolation[msg] {
  input.request.type == "04_deployment_operation"
  input.policy_check == "environment_isolation"
  not input.policy_compliance.environmentisolation
  msg := "Policy violation: environment_isolation (scope: all_environments)"
}

# Policy: secret_management (enforcement: automated, scope: all_deployments)
deny_secretmanagement[msg] {
  input.request.type == "04_deployment_operation"
  input.policy_check == "secret_management"
  not input.policy_compliance.secretmanagement
  msg := "Policy violation: secret_management (scope: all_deployments)"
}

# Policy: deployment_approval (enforcement: manual, scope: production)
deny_deploymentapproval[msg] {
  input.request.type == "04_deployment_operation"
  input.policy_check == "deployment_approval"
  not input.policy_compliance.deploymentapproval
  msg := "Policy violation: deployment_approval (scope: production)"
}

# BOUNDARY CHECK 1: Resource limits
deny[msg] {
  input.request.type == "04_deployment_operation"
  input.resource.size > 10485760  # 10MB limit
  msg := "Resource size exceeds limit (10MB) for 04_deployment"
}

# BOUNDARY CHECK 2: Rate limiting
deny[msg] {
  input.request.type == "04_deployment_operation"
  input.request.count > 1000  # Max 1000 requests
  msg := "Request count exceeds rate limit (1000) for 04_deployment"
}

# Health check
health_check := {
  "ready": ready,
  "implemented": true,
  "enforcement_enabled": true,
  "version": "v6.0",
  "root": "04_deployment",
  "capabilities": 6,
  "policies": 3
}

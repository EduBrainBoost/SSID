# SSID OPA Policy - 18_data_layer (FUNCTIONAL v6.3)
# Auto-generated from 18_data_layer/chart.yaml
# Mode: HONEST COMPLIANCE + ROOT-24-LOCK STRICT + SAFE-FIX
# Status: FUNCTIONAL

package ssid.18datalayer.v6_0

# FUNCTIONAL POLICY - ready = true
default ready := true

# Metadata
metadata := {
  "root": "18_data_layer",
  "version": "v6.0",
  "status": "functional",
  "capabilities_count": 6,
  "policies_count": 3,
  "interfaces_count": 3
}

# ============================================================
# FUNCTIONAL ENFORCEMENT RULES
# ============================================================

default allow := false

# ALLOW RULE 1: Valid authenticated operations
allow {
  input.request.type == "18_data_layer_operation"
  input.request.valid == true
  input.auth.authenticated == true
}

# ALLOW RULE 2: Read operations (less restrictive)
allow {
  input.request.type == "read"
  input.resource.root == "18_data_layer"
  input.auth.authenticated == true
}

# ALLOW RULE 3: Capability-based operations
allow {
  input.request.type == "capability_check"
  input.capability in ["data_modeling", "orm_management", "database_migrations", "query_optimization", "connection_pooling", "transaction_management"]
  input.auth.authorized == true
}

# DENY RULE 1: Missing authentication
deny[msg] {
  input.request.type == "18_data_layer_operation"
  not input.auth.authenticated
  msg := "Authentication required for 18_data_layer operations"
}

# DENY RULE 2: Invalid request
deny[msg] {
  input.request.type == "18_data_layer_operation"
  input.request.valid == false
  msg := "Invalid request for 18_data_layer - validation failed"
}

# DENY RULE 3: Missing required fields
deny[msg] {
  input.request.type == "18_data_layer_operation"
  not input.request.action
  msg := "Missing required field: action"
}

# Policy-specific enforcement rules

# Policy: migration_review (enforcement: manual, scope: all_migrations)
deny_migrationreview[msg] {
  input.request.type == "18_data_layer_operation"
  input.policy_check == "migration_review"
  not input.policy_compliance.migrationreview
  msg := "Policy violation: migration_review (scope: all_migrations)"
}

# Policy: query_timeout (enforcement: automated, scope: all_queries)
deny_querytimeout[msg] {
  input.request.type == "18_data_layer_operation"
  input.policy_check == "query_timeout"
  not input.policy_compliance.querytimeout
  msg := "Policy violation: query_timeout (scope: all_queries)"
}

# Policy: connection_limit (enforcement: automated, scope: all_pools)
deny_connectionlimit[msg] {
  input.request.type == "18_data_layer_operation"
  input.policy_check == "connection_limit"
  not input.policy_compliance.connectionlimit
  msg := "Policy violation: connection_limit (scope: all_pools)"
}

# BOUNDARY CHECK 1: Resource limits
deny[msg] {
  input.request.type == "18_data_layer_operation"
  input.resource.size > 10485760  # 10MB limit
  msg := "Resource size exceeds limit (10MB) for 18_data_layer"
}

# BOUNDARY CHECK 2: Rate limiting
deny[msg] {
  input.request.type == "18_data_layer_operation"
  input.request.count > 1000  # Max 1000 requests
  msg := "Request count exceeds rate limit (1000) for 18_data_layer"
}

# Health check
health_check := {
  "ready": ready,
  "implemented": true,
  "enforcement_enabled": true,
  "version": "v6.0",
  "root": "18_data_layer",
  "capabilities": 6,
  "policies": 3
}

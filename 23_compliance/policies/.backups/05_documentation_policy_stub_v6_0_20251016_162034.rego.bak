# SSID OPA Policy - 05_documentation (FUNCTIONAL v6.3)
# Auto-generated from 05_documentation/chart.yaml
# Mode: HONEST COMPLIANCE + ROOT-24-LOCK STRICT + SAFE-FIX
# Status: FUNCTIONAL

package ssid.05documentation.v6_0

# FUNCTIONAL POLICY - ready = true
default ready := true

# Metadata
metadata := {
  "root": "05_documentation",
  "version": "v6.0",
  "status": "functional",
  "capabilities_count": 6,
  "policies_count": 3,
  "interfaces_count": 3
}

# ============================================================
# FUNCTIONAL ENFORCEMENT RULES
# ============================================================

default allow := false

# ALLOW RULE 1: Valid authenticated operations
allow {
  input.request.type == "05_documentation_operation"
  input.request.valid == true
  input.auth.authenticated == true
}

# ALLOW RULE 2: Read operations (less restrictive)
allow {
  input.request.type == "read"
  input.resource.root == "05_documentation"
  input.auth.authenticated == true
}

# ALLOW RULE 3: Capability-based operations
allow {
  input.request.type == "capability_check"
  input.capability in ["api_documentation", "architecture_diagrams", "compliance_documentation", "user_guides", "developer_onboarding", "changelog_management"]
  input.auth.authorized == true
}

# DENY RULE 1: Missing authentication
deny[msg] {
  input.request.type == "05_documentation_operation"
  not input.auth.authenticated
  msg := "Authentication required for 05_documentation operations"
}

# DENY RULE 2: Invalid request
deny[msg] {
  input.request.type == "05_documentation_operation"
  input.request.valid == false
  msg := "Invalid request for 05_documentation - validation failed"
}

# DENY RULE 3: Missing required fields
deny[msg] {
  input.request.type == "05_documentation_operation"
  not input.request.action
  msg := "Missing required field: action"
}

# Policy-specific enforcement rules

# Policy: documentation_versioning (enforcement: automated, scope: all_docs)
deny_documentationversioning[msg] {
  input.request.type == "05_documentation_operation"
  input.policy_check == "documentation_versioning"
  not input.policy_compliance.documentationversioning
  msg := "Policy violation: documentation_versioning (scope: all_docs)"
}

# Policy: accuracy_validation (enforcement: manual_review, scope: all_docs)
deny_accuracyvalidation[msg] {
  input.request.type == "05_documentation_operation"
  input.policy_check == "accuracy_validation"
  not input.policy_compliance.accuracyvalidation
  msg := "Policy violation: accuracy_validation (scope: all_docs)"
}

# Policy: accessibility (enforcement: automated, scope: web_docs)
deny_accessibility[msg] {
  input.request.type == "05_documentation_operation"
  input.policy_check == "accessibility"
  not input.policy_compliance.accessibility
  msg := "Policy violation: accessibility (scope: web_docs)"
}

# BOUNDARY CHECK 1: Resource limits
deny[msg] {
  input.request.type == "05_documentation_operation"
  input.resource.size > 10485760  # 10MB limit
  msg := "Resource size exceeds limit (10MB) for 05_documentation"
}

# BOUNDARY CHECK 2: Rate limiting
deny[msg] {
  input.request.type == "05_documentation_operation"
  input.request.count > 1000  # Max 1000 requests
  msg := "Request count exceeds rate limit (1000) for 05_documentation"
}

# Health check
health_check := {
  "ready": ready,
  "implemented": true,
  "enforcement_enabled": true,
  "version": "v6.0",
  "root": "05_documentation",
  "capabilities": 6,
  "policies": 3
}

# SSID OPA Policy - 02_audit_logging (FUNCTIONAL v6.3)
# Auto-generated from 02_audit_logging/chart.yaml
# Mode: HONEST COMPLIANCE + ROOT-24-LOCK STRICT + SAFE-FIX
# Status: FUNCTIONAL

package ssid.02auditlogging.v6_0

# FUNCTIONAL POLICY - ready = true
default ready := true

# Metadata
metadata := {
  "root": "02_audit_logging",
  "version": "v6.0",
  "status": "functional",
  "capabilities_count": 6,
  "policies_count": 3,
  "interfaces_count": 3
}

# ============================================================
# FUNCTIONAL ENFORCEMENT RULES
# ============================================================

default allow := false

# ALLOW RULE 1: Valid authenticated operations
allow {
  input.request.type == "02_audit_logging_operation"
  input.request.valid == true
  input.auth.authenticated == true
}

# ALLOW RULE 2: Read operations (less restrictive)
allow {
  input.request.type == "read"
  input.resource.root == "02_audit_logging"
  input.auth.authenticated == true
}

# ALLOW RULE 3: Capability-based operations
allow {
  input.request.type == "capability_check"
  input.capability in ["immutable_audit_logs", "worm_storage", "blockchain_anchoring", "forensic_evidence_collection", "compliance_reporting", "merkle_tree_verification"]
  input.auth.authorized == true
}

# DENY RULE 1: Missing authentication
deny[msg] {
  input.request.type == "02_audit_logging_operation"
  not input.auth.authenticated
  msg := "Authentication required for 02_audit_logging operations"
}

# DENY RULE 2: Invalid request
deny[msg] {
  input.request.type == "02_audit_logging_operation"
  input.request.valid == false
  msg := "Invalid request for 02_audit_logging - validation failed"
}

# DENY RULE 3: Missing required fields
deny[msg] {
  input.request.type == "02_audit_logging_operation"
  not input.request.action
  msg := "Missing required field: action"
}

# Policy-specific enforcement rules

# Policy: immutability (enforcement: automated, scope: all_logs)
deny_immutability[msg] {
  input.request.type == "02_audit_logging_operation"
  input.policy_check == "immutability"
  not input.policy_compliance.immutability
  msg := "Policy violation: immutability (scope: all_logs)"
}

# Policy: retention (enforcement: automated, scope: all_logs)
deny_retention[msg] {
  input.request.type == "02_audit_logging_operation"
  input.policy_check == "retention"
  not input.policy_compliance.retention
  msg := "Policy violation: retention (scope: all_logs)"
}

# Policy: integrity_verification (enforcement: continuous, scope: all_logs)
deny_integrityverification[msg] {
  input.request.type == "02_audit_logging_operation"
  input.policy_check == "integrity_verification"
  not input.policy_compliance.integrityverification
  msg := "Policy violation: integrity_verification (scope: all_logs)"
}

# BOUNDARY CHECK 1: Resource limits
deny[msg] {
  input.request.type == "02_audit_logging_operation"
  input.resource.size > 10485760  # 10MB limit
  msg := "Resource size exceeds limit (10MB) for 02_audit_logging"
}

# BOUNDARY CHECK 2: Rate limiting
deny[msg] {
  input.request.type == "02_audit_logging_operation"
  input.request.count > 1000  # Max 1000 requests
  msg := "Request count exceeds rate limit (1000) for 02_audit_logging"
}

# Health check
health_check := {
  "ready": ready,
  "implemented": true,
  "enforcement_enabled": true,
  "version": "v6.0",
  "root": "02_audit_logging",
  "capabilities": 6,
  "policies": 3
}
